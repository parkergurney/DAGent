# Semantic-quality orchestrator benchmark

This task contains one or more pinned open-source projects under `repos/`.
Complete every graph task assigned to you. Read the relevant public tests and
source code, implement the requested behavior, run the public tests that are
useful for the change, and commit the fix in the repository checkout.

Do not inspect, recreate, or modify anything under `/tests`; that directory is
reserved for Harbor's separate verifier. Do not claim completion without a
working implementation and a committed change. Keep each change inside the
task's declared project and write scope. End every completed task response with
`DONE_CLAIM`.
